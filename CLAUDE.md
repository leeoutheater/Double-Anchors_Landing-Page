# CLAUDE.md — 프로젝트 작업 지침

이 파일은 Claude Code가 매 세션 자동으로 읽는 프로젝트 지침입니다.

## 프로젝트 한 줄 요약

청소년 진로체험 예술교육 프로그램 **"더블앵커(Double Anchors)"** 의 단일 파일 정적 랜딩 페이지.
모든 코드는 `index.html` 하나에 들어 있음 (HTML + 인라인 CSS + 인라인 JS, 빌드 단계 없음).
자세한 구조·상태는 **`HANDOFF.md`** 참고.

---

## ⭐ "VPP 세션" 규칙 (중요)

사용자가 아래와 **같거나 비슷한 표현**으로 새 작업을 요청하면, 그것은 **"VPP 세션"** 을 의미한다.

> 트리거 예시 (이와 유사한 한국어 표현 모두 포함):
> - "VPP 세션을 만들어줘"
> - "VPP 세션 시작"
> - "HANDOFF.md 기준으로 이어가자"
> - "이전 작업 인수인계해서 에러 없이 완성해줘"
> - "이어서 작업해줘 / 이어받아서 마무리해줘"

**VPP 세션 = "HANDOFF.md를 기준으로 이전 작업을 인수인계받아, 에러 없이 완성한다"**

### VPP 세션을 받으면 따를 절차

1. **읽기** — 가장 먼저 `HANDOFF.md` 전체를 읽고, 특히 §4(남은 작업)와 §6(인수인계 로그 최상단)으로 현재 맥락을 파악한다.
2. **확인** — §4에 남은 작업이 있으면 그것을 이어서 진행한다. 없거나 모호하면 사용자에게 무엇을 이어갈지 짧게 확인한다.
3. **작업** — `index.html`을 수정한다. 단일 `<script>` 블록·인라인 스타일·5개 언어(`ko`/`en`/`ru`/`zh`/`vi`) i18n 구조를 깨지 않는다.
4. **검증** — `HANDOFF.md` §5의 체크리스트로 "에러 없이" 동작하는지 확인한다 (콘솔 에러 0, 언어 전환·영상 자동재생·모달·FAQ·캔버스 정상).
5. **갱신** — 작업 종료 시 `HANDOFF.md`를 갱신한다:
   - 끝낸 항목은 §4 → §3(DONE)으로 이동
   - 새로 생긴 미해결 항목은 §4에 추가
   - §6 인수인계 로그 **최상단**에 날짜와 요약 한 줄 추가
6. **커밋·푸시** — 명확한 한국어 커밋 메시지로 지정 브랜치에 커밋·푸시한다. (PR은 사용자가 명시적으로 요청할 때만 생성)

> 핵심 원칙: **"에러 없이 완성"** 이 목표다. 추측으로 기능을 깨뜨리지 말고, 변경 후에는 반드시 §5 검증을 거친다.

---

## 코드 컨벤션

- **언어**: UI 텍스트·주석·커밋 메시지는 한국어. 코드 식별자는 기존 스타일(camelCase 함수, 한국어 주석 구분선 `─── n. 제목 ───`) 유지.
- **i18n**: 새 텍스트를 추가하면 반드시 `data-i18n="키"`를 달고 `i18n` 사전의 **5개 언어 전부**에 키를 추가한다. 키 누락 시 해당 언어에서 텍스트가 안 바뀜.
- **영상**: 새 아카이브 영상은 `window.ARCHIVE_VIDEOS`에 `mp4Url`/`gdriveId`로 대체 소스를 등록할 수 있다. 우선순위: mp4Url → gdriveId → youtube fallback.
- **의존성 추가 금지(원칙)**: 빌드 없는 단일 파일 구조를 유지한다. 새 라이브러리는 꼭 필요할 때 CDN으로만, 사용자 확인 후.

## 하면 안 되는 것

- `index.html`을 여러 파일로 쪼개거나 빌드 시스템을 도입하지 않는다 (사용자 명시 요청 없이).
- 5개 언어 i18n 구조나 단일 `<script>` 블록 구조를 임의로 변경하지 않는다.
- PR을 임의로 만들지 않는다 (요청 시에만).
